
SKIP_SYNC=

# Everything else (MONGO_URI, model/tool tokens, workspace dirs) is owned by
# @mida/claude-config and seeded from this file on first boot — see
# packages/claude-config for the full list.
